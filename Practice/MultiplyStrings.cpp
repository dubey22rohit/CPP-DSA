#include <iostream>
#include <string>
using namespace std;

string multiply(string num1, string num2) {}

int main() {
  string num1, num2;
  getline(cin, num1);
  getline(cin, num2);

  cout << multiply(num1, num2);
  return 0;
}

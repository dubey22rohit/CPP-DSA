#include <iostream>
#include <unordered_map>
using namespace std;

string findSubstring(const string &str, const string &pattern) {
  int ws = 0;
  unordered_map<char, int> hm;
  for (int we = 0; we < str.size(); ++we) {
  }
}

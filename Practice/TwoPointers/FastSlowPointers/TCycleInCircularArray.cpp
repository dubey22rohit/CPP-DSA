#include <iostream>
using namespace std;

// Cycle in a Circular Array (hard)
bool loopExists(vector<int> &arr) {
  int slow = 0, fast = 0;
  while (slow != fast) {
    // TODO
  }
}
int main(int argv, char *argc[]) { return 0; }
